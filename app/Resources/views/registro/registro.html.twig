{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h3>PERFIL</h3>
            {{ form_start(form) }}
            {{ form_label(form.correo) }}
            <div class="input-group mb-3">
            {{ form_widget(form.correo) }}
                <div class="input-group-append">
                    <span class="input-group-text">@uabc.edu.mx</span>
                </div>
            </div>
            {{ form_errors(form.correo) }}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}